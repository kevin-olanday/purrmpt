generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model PurrmptCounter {
  id    Int    @id @default(autoincrement())
  date  String @unique
  count Int    @default(0)
}